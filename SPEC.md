# サクトレ（sakutore）仕様書

> 本ドキュメントは現行コードベースから読み取った仕様をまとめたものです。

## 1. 概要

- **名称**: サクトレ（キャッチコピー「サクッとトレーニングメニュー作成」）
- **目的**: いくつかの質問に答えるだけで、AIが「今日1回分」の筋トレメニューを自動生成する無料Webアプリ
- **マスコット**: サクラ（🌸 `/sakura.png`）
- **公開URL**: https://sakutore.jp
- **姉妹サービス**: サクメシ（食事プラン）/ サクサプ（サプリ診断）へ各所から導線
- **収益**: Google AdSense（`ca-pub-2851344861391489`）、ライザップのA8アフィリエイト

## 2. 技術スタック

| 項目 | 内容 |
|------|------|
| フレームワーク | Next.js 14.2.35（App Router）|
| 言語 | TypeScript / React 18 |
| スタイリング | Tailwind CSS 3.4 |
| AI | Anthropic SDK（モデル: `claude-haiku-4-5-20251001`）|
| 画像保存 | html2canvas |
| フォント | Noto Sans JP（next/font）|
| データ永続化 | ブラウザ localStorage のみ（サーバDBなし）|
| PWA | manifest + ServiceWorker 登録あり |
| デプロイ | Vercel |

## 3. 画面・ルート構成

### 主要フロー
- `/` … トップ。今日のおすすめ・回復状況・メニュー履歴・カレンダー・各ツールへの導線
- `/questions` … 5問のステップ形式アンケート
- `/result` … 生成結果表示（`ResultClient.tsx`）
- `/chat` … サクラとの会話形式メニュー作成（トップからは未リンク・存在する機能）

### ツール
- `/rm-calculator` … RM換算計算（ビッグ3、Epley式、2〜15RM換算表＋強度ゾーン）
- `/calorie-calculator` … METs式の消費カロリー計算（8種目）＋脂肪換算（kcal÷7）
- `/weight-checker` … 性別・レベル別の適正重量診断（体重倍率テーブル）

### コンテンツ / SEO
- `/videos` および `/videos/{chest,back,shoulder,arm,leg,abs,women}` … 部位別トレーニング動画まとめ
- `/column` ＋ 20本のコラム記事（protein, sleep, rest, frequency, over40, 女性向けなど）
- `/guide`（使い方）、`/faq`、`/about`（運営者情報）、`/privacy`、`/terms`

### システム
- `/api/generate` … 質問回答からメニュー生成（POST）
- `/api/chat` … チャット形式メニュー生成（POST）
- `/api/og` … OGP画像動的生成
- `sitemap.ts` / `robots.ts` / `manifest.ts`（PWA）

## 4. アンケート（5問）`src/lib/questions.ts`

| # | 質問 | 形式 | 選択肢 |
|---|------|------|--------|
| 1 | トレーニングの目的 | 単一 | 筋肥大 / ダイエット / 健康維持 / スポーツパフォーマンス向上 |
| 2 | 今日のトレーニング時間 | 単一 | 30分以内 / 30〜60分 / 60〜90分 / 90分以上 |
| 3 | トレーニング経験 | 単一 | 初心者(〜半年) / 中級者(半年〜2年) / 上級者(2年以上) |
| 4 | 鍛えたい部位 | 複数選択 | 胸/肩/腕(二頭)/腕(三頭)/背中(広背筋)/背中(僧帽筋)/腹筋/腰/お尻/脚(前もも)/脚(裏もも)/こだわりなし |
| 5 | トレーニング環境 | 単一 | ジム / 自宅(ダンベルあり) / 自宅(自重のみ) |

回答は `?q0=...&q4=...` のクエリで `/result` に渡される。

## 5. メニュー生成ロジック `src/app/api/generate/route.ts`

- **入力**: `answers`(5要素必須) ＋ `recovery`(部位別回復状況、任意)
- **動的にプロンプトを組み立て**:
  - 種目数: 30分→4種目、60分→5〜6、それ以上→6〜7
  - レップ数: 筋肥大6〜12 / ダイエット12〜20 / 健康10〜15 / その他8〜15
  - 経験レベルで種目選択方針を変更（初心者=コンパウンド/マシン中心 等）
  - 環境の器具のみ使用
  - 回復済み（2日以上 or 未実施）の部位を優先、当日・前日実施部位は回避
- **本日1回分のみ**（`days` は必ず1要素）、鍛える部位は1〜2部位に絞る
- **出力形式（JSON）**:
  ```json
  {"title":"絵文字+名前","description":"狙い1〜2文",
   "days":[{"day":"本日のメニュー","label":"部位","exercises":[{"name":"種目名","sets":3,"reps":"10回","rest":"90秒"}]}]}
  ```
- **信頼性**: timeout 20秒・自動リトライ1回、stop_reason検証、JSON抽出、エラー種別ごとに日本語メッセージ＋HTTPステータス（429/502/503/500）

> 補足: `src/lib/mockResult.ts` に目的×環境別の固定メニュー6種が定義されているが、現状の生成フローはAPI（Claude）経由。mockResult はフォールバック用テンプレ／型定義（`TrainingMenu`等）として残存。

## 6. 結果画面の機能 `ResultClient.tsx`

- **ローディング画面**: サクラのアニメ＋疑似プログレスバー＋筋トレ豆知識8種をローテーション
- **種目カード（ExerciseCard）**:
  - セット×レップ表示
  - **インターバルタイマー**: rest時間からカウントダウン、終了時にビープ音（Web Audio）
  - **種目詳細**（`exercises.ts` に25種目）: やり方手順・フォーム注意点・効かせるコツ（プロ目線）・YouTube検索リンク。折りたたみ時はコツ1行をチラ見せ
  - **実績メモ**: セットごとに重量(kg)×回数を記録・localStorage保存・追加/削除可
- **トレーニング完了ボタン**: 押すと当日を記録（ストリーク・カレンダー連動）
- **シェア**: X / LINE / 画像ダウンロード（html2canvas）
- **キャッシュ**: 同条件×同日なら再生成せず保存メニューを再利用（日付が変われば作り直し）
- **履歴からの表示**: `?source=history&hid=` で過去メニューを再生成せずそのまま表示
- **チャット結果**: `?source=chat` で localStorage のチャットメニューを表示
- **広告/導線**: サクメシ・サクサプ・ライザップ（A8アフィリエイト）バナー

## 7. チャット機能 `src/app/chat/page.tsx` ＋ `/api/chat`

- LINE風UI。サクラが会話で目的/頻度/時間/経験/部位/環境を順次ヒアリング
- 選択肢チップ（`<options>` タグ）を提示、全情報が揃うと `<menu>` JSON を出力
- 生成後 localStorage に保存し `/result?source=chat` へ自動遷移
- 親しみやすい口調（敬語なし）。頻度に応じ最大3日分（chat版は複数日対応）

## 8. localStorage データ設計

| キー | 内容 |
|------|------|
| `sakutore_saved_menu` / `_params` / `_date` | 当日メニューのキャッシュ |
| `sakutore_menu_history` | メニュー履歴（最大10件、id/date/params/title/menu）|
| `sakutore_workout_log` | トレーニング実施日の配列（YYYY-MM-DD）|
| `sakutore_training_memo` | 種目別の実績記録（`日付_種目名` キー）|
| `sakutore_chat_menu` | チャット生成メニュー |

- **データリセットボタン**（トップ最下部）: 上記キーを一括削除

## 9. 回復状況サジェスト `src/lib/muscleGroupSuggestion.ts`

- 実績メモから種目→部位（胸/背中/脚/肩/腕/腹筋）をマッピングし、部位ごとの最終トレ日を算出
- ステータス: `ready`(2日以上/準備OK) / `almost`(1日/もうすぐ) / `recovering`(当日/回復中) / `unknown`(未記録)
- **今日のおすすめ部位**: 最も長く休めている部位を提案（トップの `TodaySuggestion`）
- この回復状況を `/api/generate` に渡し、AIが鍛える部位選択に反映

## 10. カレンダー `WorkoutCalendar.tsx`

- 月表示、トレ実施日を緑丸、当日をオレンジ枠で表示
- 月間トレ日数を集計表示、前月/翌月ナビ
- メニュー履歴がある日はタップで該当メニュー（`?source=history`）を再表示

## 11. 計算ツール詳細

- **RM換算**: 1RM = 重量×(1+回数/30)（Epley式）。2〜15RM換算表＋目的別ゾーン色分け
- **消費カロリー**: METs×体重×時間(h)。脂肪換算 = kcal÷7。種目8種（ベンチ6.0〜縄跳び10.0等）
- **適正重量診断**: 性別×レベル（初心者/初級/中級/上級）×ビッグ3の体重倍率テーブルで現在値と次レベル目標値・差分を提示

## 12. SEO / メタ

- 全ページ日本語、OGP/Twitterカード設定、Google Search Console認証済み
- 各ツールページに個別 `layout.tsx` でメタデータ設定
- sitemap はコラムディレクトリを動的走査して自動生成
